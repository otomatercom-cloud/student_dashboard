# Server-side only (not exposed to the browser) — used by the Next.js
# API routes that proxy requests to Odoo. No NEXT_PUBLIC_ prefix since
# the browser itself never needs to know this URL directly anymore;
# it only ever talks to this app's own /api/* routes.
ODOO_BASE_URL=http://localhost:8019
